import NavButton from './NavButton';
import NavLinks from './NavLinks';
const Nav = () => {
    return (
        <nav>
            <NavButton />
            <NavLinks />
        </nav>
    )
}

export default Nav
